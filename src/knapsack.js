/**
 * @description 01背包问题
 */
function knapsack01(n, v, list) {
  // let list = weights.map((item, index) => ({ weight: item, value: values[index] })).sort((a, b) => a.weight - b.weight)
  // console.log(list)
  let value = 0
  for (let i = 0; i < n; i++) {
    // const element = array[i];
    for (let j = v; j >= 0; j--) {
      // const element = array[j]
    }
  }
}
knapsack01([2, 2, 6, 5, 4], [6, 3, 5, 4, 6])
